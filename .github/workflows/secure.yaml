name: Secure Pipeline

on:
  push:
    branches: ["main"]   # ✅ Only trusted branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read     # ❌ No id-token

    steps:
      - uses: actions/checkout@v3

      - name: Safe Step
        run: echo "No AWS access"
